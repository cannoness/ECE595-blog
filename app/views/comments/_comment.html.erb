<%= div_for comment do %>
  <p>
    <strong>
      Posted <%= time_ago_in_words(comment.created_at)%>, by <%= h(comment.user.email)%>
    </strong>
    <br />
    <%= h(comment.body) %>
  </p>
<% end %>
